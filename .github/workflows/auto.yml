name: 'GitHub Actions Bot'

on:
    push:
        branches:
            - dev

jobs:
        auto_deploy:
        runs-on: ubuntu-20.04
        steps:
          - name: 'Checkout'
            uses: actions/checkout@v2
          - name: 'Get fossil'
            run:  bash ./git.sh
            shell: bash

